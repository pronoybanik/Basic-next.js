import React from 'react';

export const metadata = {
    title: 'About | Hero',
    description: 'Hero Website',
}

const About = () => {
    return (
        <div>
            About page
        </div>
    );
};

export default About;