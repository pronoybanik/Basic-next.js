import React from 'react';

export const metadata = {
    title: 'profile | Hero',
    description: 'Hero Website',
}

const profile = () => {
    return (
        <div>
            profile Page
        </div>
    );
};

export default profile;