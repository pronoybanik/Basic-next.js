import React from 'react';

export const metadata = {
    title: 'ManageProduct | Hero',
    description: 'Hero WebSite',
}

const ManageProduct = () => {
    return (
        <div>
            ManageProduct page
        </div>
    );
};

export default ManageProduct;